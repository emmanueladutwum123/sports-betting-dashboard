line-length = 100
target-version = "py311"

[lint]
select = ["E", "F", "W", "I", "UP", "B"]
ignore = ["E501"]

[lint.per-file-ignores]
"app.py" = ["E402"]        # imports must follow load_dotenv()
"tests/*" = ["E402"]       # sys.path manipulation precedes imports
